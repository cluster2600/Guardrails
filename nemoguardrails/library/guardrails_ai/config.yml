models:
  - type: main
    engine: openai
    model: gpt-3.5-turbo

rails:
  input:
    flows:
      - guardrails_ai check input
  output:
    flows:
      - guardrails_ai check output

# Optional configuration for Guardrails AI
guardrails_ai:
  enabled: true
